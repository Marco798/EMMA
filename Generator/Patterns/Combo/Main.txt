using EMMA.Commons;
using System.Reflection;

namespace %%NAME_SPACE%% {
	public class %%COMBO_NAME%%_Combo : ComboBase {
		internal %%COMBO_NAME%%_Combo(string value) : base(value) { }

%%VALUE_FIELD%%

        public static string[] GetNames() {
            return typeof(%%COMBO_NAME%%_Combo).GetFields(BindingFlags.Public | BindingFlags.Static).Select(f => f.Name).ToArray();
        }

        public static List<%%COMBO_NAME%%_Combo> GetValues() {
            return [
%%GET_VALUE_FIELD%%
            ];
        }
	}

}
